import { Routes } from '@angular/router';
import { DashboardComponent } from '@/gestionale/dashboard/dashboard.component';

export const routes: Routes = [{ path: '', component: DashboardComponent }];

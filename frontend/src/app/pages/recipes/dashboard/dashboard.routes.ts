import { Routes } from '@angular/router';
import { DashboardComponent } from '@/pages/recipes/dashboard/dashboard.component';

export const routes: Routes = [{ path: '', component: DashboardComponent }];
